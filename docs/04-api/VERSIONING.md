# API Versioning

Current Version

v1

Pattern

/api/v1/

Example

GET /api/v1/raffles

Future

/api/v2/

Rules

Never introduce breaking changes inside the same version.

New features should remain backwards compatible whenever possible.