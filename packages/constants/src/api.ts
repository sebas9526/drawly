/**
 * Shared API constants mirroring docs/04-api/VERSIONING.md and RESPONSE_FORMAT.md.
 * Moved here (from @drawly/config) so all cross-cutting constants live in one place.
 */

export const API_VERSION = 'v1';
export const API_V1_PREFIX = '/api/v1';
export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_SIZE = 20;
