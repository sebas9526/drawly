/**
 * Moved to @drawly/utils (shared by the admin and public ticket views).
 * Re-exported here for backwards compatibility; prefer importing from
 * '@drawly/utils' directly.
 */
export { formatTicketNumber } from '@drawly/utils';
